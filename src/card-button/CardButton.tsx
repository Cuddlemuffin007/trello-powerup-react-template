import React from "react";

// import { useProvidedTrello } from "../hooks";

export const CardButton = (): JSX.Element => {
    // const t = useProvidedTrello();
    return (
        <div>
            <p>Card Button IFrame content</p>
        </div>
    );
};
